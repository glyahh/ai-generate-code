interface:
  display_name: "humanizer"
  short_description: "Rewrite to sound natural"
  default_prompt: "Use $humanizer to rewrite my text to be natural, concise, and professional."

policy:
  allow_implicit_invocation: true
